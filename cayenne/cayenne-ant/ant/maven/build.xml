<?xml version="1.0"?>
 
<!-- ================================================= -->
<!--            Maven Bundle Buildfile                 -->
<!-- ================================================= -->
<project name="cayenne-maven" default="all" basedir="../..">
	
	<property file="${basedir}/default.properties"/>
	<import file="${basedir}/ant/import/paths.xml"/>
	
	<property name="build" value="${build.base}/maven"/>
	<property name="dist"
		value="${dist.base}/${project.name}-${project.version}"/>
	
	<target name="all" depends="build-cayenne">
		
		<antcall target="bundle">
			<param name="stage" value="${build}/maven/cayenne"/>
			<param name="base.name" value="${project.name}"/>
			<param name="cayenne.jar" value="cayenne.jar"/>
		</antcall>
		
		<antcall target="bundle">
			<param name="stage" value="${build}/cayenne-nodeps"/>
			<param name="base.name" value="${project.name}-nodeps"/>
			<param name="cayenne.jar" value="cayenne-nodeps.jar"/>
		</antcall>
	</target>
	
	<target name="clean">
		<delete dir="${build}"/>
		
		<delete>
			<fileset dir="${dist.base}" includes="*-bundle.jar"/>
		</delete>
	</target>
	
	<target name="bundle">
		
		<mkdir dir="${stage}"/>
		
		<copy file="ant/maven/${base.name}.pom" tofile="${stage}/pom.xml">
			<filterset>
				<filter token="PROJECT-VERSION" value="${project.version}"/>
			</filterset>
		</copy>
		
		<copy file="${cayenne.other}/licenses/LICENSE.txt" tofile="${stage}/LICENSE.txt"/>
		<copy
			file="${dist.base}/${project.name}-${project.version}/lib/${cayenne.jar}"
			tofile="${stage}/${base.name}-${project.version}.jar"/>
		
		<jar jarfile="${stage}/${base.name}-${project.version}-sources.jar">
			<fileset dir="${cayenne.java}/src/cayenne/java" includes="**/*.java"/>
			<fileset dir="${cayenne.java.1_5}/src/cayenne/java" includes="**/*.java"/>
		</jar>
		
		<jar jarfile="${dist.base}/${base.name}-${project.version}-bundle.jar">
			<fileset dir="${stage}"/>
		</jar>
	</target>
	
	<target name="build-cayenne">
		<ant antfile="build.xml" target="dist-bin"/>
	</target>	
</project>