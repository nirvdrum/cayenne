<document>
<title>DataPort Code Samples</title>
<body>
    <section name="Data Port Code Samples">
    
<p>How to manually load Cayenne project from file (from CayenneDataPort.java):</p>
<source>
import org.objectstyle.cayenne.conf.Configuration;
import org.objectstyle.cayenne.conf.FileConfiguration;
import java.io.File;
...
File cayenneProjectFile;
...
FileConfiguration configuration = new FileConfiguration();
configuration.addFilesystemPath(cayenneProjectFile.getParentFile());
Configuration.initializeSharedConfiguration(configuration);
</source>


<p>How to sort a list of entities in the database insert order (from DataPort.java):</p>
<source>
import org.objectstyle.cayenne.access.DataNode;
import java.util.List;
...
DataNode dataNode;
List entities;
...
// "false" indicates "insert order", "true" would've indicated "delete order"
dataNode.getDependencySorter().sortDbEntities(entities, false);
</source>

<p>Full source code contains more interesting pieces that would not fit into a small 
code snip, like using delegation to decouple the actual DataPort processing algorithm from 
various configuration decisions, writing your own ant task, reading data from one DataNode 
and immediately writing it into another, etc.
</p>
    </section>
</body>
</document>
