{
    attributes = (
        {
            columnName = ARTIST_ID; 
            externalType = int; 
            name = artistId; 
            valueClassName = NSNumber; 
            valueType = i; 
        }, 
        {
            columnName = ARTIST_NAME; 
            externalType = varchar; 
            name = artistName; 
            valueClassName = NSString; 
            width = 255; 
        }, 
        {
            allowsNull = Y; 
            columnName = DATE_OF_BIRTH; 
            externalType = datetime; 
            name = dateOfBirth; 
            valueClassName = NSCalendarDate; 
        }
    ); 
    attributesUsedForLocking = (artistId); 
    className = org.objectstyle.art.Artist; 
    classProperties = (artistName, dateOfBirth, artistExhibitArray, paintingArray); 
    externalName = ARTIST; 
    fetchSpecificationDictionary = {}; 
    name = Artist; 
    primaryKeyAttributes = (artistId); 
    relationships = (
        {
            deleteRule = EODeleteRuleCascade; 
            destination = ArtistExhibit; 
            isToMany = Y; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = artistId; sourceAttribute = artistId; }); 
            name = artistExhibitArray; 
            ownsDestination = Y; 
            propagatesPrimaryKey = Y; 
        }, 
        {
            deleteRule = EODeleteRuleCascade; 
            destination = Painting; 
            isToMany = Y; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = artistId; sourceAttribute = artistId; }); 
            name = paintingArray; 
        }
    ); 
}
