<?xml version="1.0"?>
<document>
    <title>Developer</title>

    <properties>
        <author>Andrei Adamchik</author>
    </properties>

    <body>
        <!-- CVS section -->
        <section name="CVS Access">
            <p>To get Cayenne code via CVS, follow the instructions on 
            <a href="http://sourceforge.net/cvs/?group_id=48132">this page</a>. 
            Module name is <code>cayenne</code>. Also you may find <a href="http://objectstyle.org/developer/cvs.html">
            this page</a> helpful if you are trying to setup CVS/SSH
            access on Windows.</p>
        </section>
        
        <!-- Compile section -->
        <section name="Compiling Cayenne">
            <p>To compile Cayenne, run ant (must be version 1.4.1 or higher) on the main buildfile:</p>
            <source>cd cayenne
ant</source>

            <p>To run unit tests do the following (commands are given for UNIX, on Windows do something similar):</p>
            <source>export CAYENNE_HOME=`pwd`/dist/cayenne
$CAYENNE_HOME/bin/runtests.sh</source>
        </section>
    </body>
</document>

